@page "/"
@using GameOverBoardGame.Model;

<h1>GameOver board game</h1>

@code {
    GameBoard gameBoard = new GameBoard(2);
}

@for (int i = 0; i < 7; i++)
{
    @for (int j = 0; j < 7; j++)
    {
        <span>@gameBoard.Board[i, j].Style</span>
    }
    <br />
}